export interface BlockContentTypes {
  postText: string;
  postContent: string;
  img: any;
}

export interface OurValuesTypes {
  title: string;
  img: any; 
  text: string;
}

export interface sliderComponents {
  title:string;
  img:any;
}
